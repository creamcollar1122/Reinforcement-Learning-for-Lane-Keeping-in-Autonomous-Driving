{
    "name": "Reinforcement Learning Environment",
    "dockerFile": "Dockerfile",
    "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
    ],
    "postCreateCommand": "pip install -r src/requirements.txt",
    "settings": {
        "python.pythonPath": "/usr/local/bin/python"
    }
}
